#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../noodlejs/bin/noodle-server" "$@"
  ret=$?
else 
  node  "$basedir/../noodlejs/bin/noodle-server" "$@"
  ret=$?
fi
exit $ret
